# Compatibility

agy-reader is verified against the Antigravity CLI (`agy`) local session format
recorded below. This file is generated by the `agy-format-audit` skill and is
replaced wholesale on a qualified passing audit (`audit_format.sh --record`).
Do not hand-edit.

- **agy version:** 1.1.9
- **Verified on:** 2026-07-31
- **agy-reader commit:** `33959d2` (clean working tree)
- **Schema:** user_version=1, 7 tables, indices: idx_steps_status,idx_steps_step_type
- **Schema fingerprint:** `sha256:1ca98426f561fe73223c8620a238405030fdb3014444d970e1300a6009f72f43`
- **Sidecar shape fingerprint:** `sha256:876633fdeb141a8632f6dc8e62124d8c9ea085524f557d9e120fb05abfb732e7`
- **Sidecar corpus scope:** `explicit-version-scoped`
  — 107 sidecars from `~/.gemini/antigravity-cli/conversations`. Provenance
  caveat for this record: only 3 were serialized under 1.1.9 (the rest carry
  over from the 1.1.6 sweep of 2026-07-24 plus 2 from 1.1.8). The digest
  moved vs the 1.1.8 record purely because those 3 introduced one additive
  subtree, `steps.[].checkpoint.subagentSnapshots` (subagent spec + result
  metadata); the 104-sidecar pre-1.1.9 subset still reproduces the 1.1.8
  digest byte-identically, so nothing was removed from previously exercised
  paths. A removal in a path the 3 new sidecars do not exercise was **not**
  swept; the 1.1.9 changelog is headless/hooks/TUI/permissions only and gives
  no reason to expect one. Redo the full daemon re-sync sweep before trusting
  this digest for removals.
  — deterministic digest over the trajectory JSON's key-structure (see
  `internal/shapefp`); catches daemon RPC-response reshaping the schema
  fingerprint cannot see. Corpus-dependent: by default this is the sidecar
  paired with the newest DB; set `AGY_SIDECAR_CORPUS` to a stable curated
  corpus generated by the version under audit.
- **IDE surface:** the Antigravity IDE (`~/.gemini/antigravity`) writes the
  same conversation `.db` schema, verified byte-identical against this
  fingerprint at Antigravity 2.2.1 (2026-07-02); only `trajectory_meta.source`
  differs (IDE=1, CLI=17). Spot-check the IDE copy read-only with
  `ANTIGRAVITY_CLI_ROOT=$HOME/.gemini/antigravity audit_format.sh` — never
  `--record` from an IDE root.
